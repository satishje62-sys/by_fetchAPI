# QuoteSpark

A responsive random quote generator built with HTML, CSS, JavaScript, the Fetch API, and DOM manipulation.

Open `index.html` in a browser and select **New quote** to load a random quote.
